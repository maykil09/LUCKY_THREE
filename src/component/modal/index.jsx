import React from "react";
import {
    Button,
    Dialog,
    DialogActions,
    DialogContent,
    DialogContentText,
    DialogTitle,
    TextField
} from "@mui/material";

function Modal({isModalOpen, setIsModalOpen, title}) {
    return <div>Modal</div>;
}

export default Modal;
